# MongoDB Configuration
# Replace with your actual MongoDB Atlas connection string
MONGODB_URL=mongodb+srv://pradhyumnsthakur_db_user:pRklglnCM9NmVRMU@vibevirtuoso-cluster.sshsgu8.mongodb.net/?retryWrites=true&w=majority&appName=vibevirtuoso-cluster
DATABASE_NAME=vibevirtuoso

# Connection Pool Settings (optional - defaults are fine for development)
DB_MIN_POOL_SIZE=10
DB_MAX_POOL_SIZE=100

# Timeout Settings (optional)
DB_SERVER_SELECTION_TIMEOUT=5000
DB_CONNECT_TIMEOUT=10000
DB_SOCKET_TIMEOUT=10000

# Application Name
DB_APP_NAME=VibeVirtuoso

# Retry Settings
DB_RETRY_WRITES=true

# Other existing environment variables
# GEMINI_API_KEY=your_gemini_api_key_here (Removed - now using built-in training)